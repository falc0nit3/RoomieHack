<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>           <!--Don't show the ActionBar - Use the new toolbar for material design-->
    </style>

    <style name="AppNameScreen" parent="@style/TextAppearance.AppCompat.Display3">
        <item name="android:textColor">#fff</item>
    </style>

    <style name="welcomeText" parent="@style/TextAppearance.AppCompat.Display2">
        <item name="android:textColor">#333</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="nameText" parent="@style/TextAppearance.AppCompat.Display2">
        <item name="android:textColor">#333</item>
        <item name="android:textSize">22sp</item>
    </style>

    <!--Material Design Changes-->
    <style name="ActionBarThemeOverlay" parent="">
        <item name="android:textColorPrimary">#fff</item>
        <item name="colorControlNormal">?actionBarIconColor</item>
        <item name="colorControlHighlight">#000</item>
    </style>

    <style name="HeaderBar">
        <item name="android:background">?colorPrimary</item>
        <item name="android:elevation">@dimen/headerbar_elevation</item>
    </style>

    <style name="ActionBarPopupThemeOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="ActionBar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#fff</item>
        <item name="android:textSize">18sp</item>
    </style>
</resources>
